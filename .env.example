# Application Mode: development or production
# development: Runs frontend dev server with hot reload
# production: Only builds frontend, backend serves static files
APP_MODE=development

# Port Configuration
BACKEND_PORT=3000
FRONTEND_PORT=5173

# Backend Environment Variables
NODE_ENV=production
DATABASE_URL=file:/data/dev.db

# Manga storage path on host machine (will be mounted to /manga in container)
MANGA_STORAGE_PATH=/mnt/d/Manhwa

# Vhost domain for nginx and frontend
VHOST_DOMAIN=mangareader.local